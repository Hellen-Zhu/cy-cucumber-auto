{
  "name": "cy-cucumber",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "cypress:open": "cypress open",
    "cypress:run": "cypress run --headless",
    "regression": "node ./utils/regression.js"
    
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@badeball/cypress-cucumber-preprocessor": "^22.2.0",
    "@bahmutov/cypress-esbuild-preprocessor": "^2.2.5",
    "cypress": "^13.7.3",
    "dotenv": "^17.2.1",
    "esbuild": "^0.25.8",
    "papaparse": "^5.5.3"
  },
  "cypress-cucumber-preprocessor": {
    "stepDefinitions": [
      "cypress/e2e/**/*.steps.{js,ts}"
    ]
  },
  "dependencies": {
    "cypress-cloud": "2.0.0-beta.1"
  }
}
